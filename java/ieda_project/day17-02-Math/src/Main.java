/**
 * @author ljw
 * @version 1.0
 */
public class Main {
    public static void main(String[] args) {
        // Math类
        // ·基本介绍
        //Math类包含用于执行基本数学运算的方法，如初等指数、对数、平方根和三角函数。
        //）方法一览（均为静态方法）
        //abs(double a)
        //返回 double 值的绝对值。
        //abs(float a)
        //返回 f1oat 值的绝对值。
        //aBS(int a)
        //返回 int 值的绝对值。
        //BS(long a)
        //返回 1ong值的绝对值。
        //acos(double a)
        //返回一个值的反余弦；返回的角度范围在0.0到pi之间
        //asin(double a)
        //韩顺平
        //返回一个值的反正弦；返回的角度范围在 -pi/2到 pi/2 之间。
        //教育
        //atan(double a)
        //返回一个值的反正切；返回的角度范围在 -pi/2 到pi/2 之间
        //atan2 (double y,double x)
        //）转换成极坐标（r，theta)，返回所得角 theta。
        //cbrt(double a)
        //返回double值的立方根。
        System.out.println("Hello world!");

        // Arrays类 ArraysMethod01.java
        // Arrays类常见方法应用案例
        // Arrays里面包含了一系列静态方法，用于管理或操作数组(比如排序和搜索)
        /*
        * 1.toString返回数组的字符串形式  Arrays.toString(arr)
        * 2.sort排序（自然排序和定制排序）
        * 3.binarySearch通过二分搜索素法进行查找，要求必须排好序
        * 4.copyOf 数组元素的复制
        * 5.fill 数组元素的填充
        * 6.equals 比较两个数组的元素是否完全一致
        * 7.asList 将一组值转换为list
        * */
    }
}